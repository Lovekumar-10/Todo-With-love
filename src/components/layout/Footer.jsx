export default function Footer() {
  return (
    <footer className="border-t border-[color:var(--border-soft)] bg-[color:var(--paper-card)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 h-12 flex items-center justify-center text-xs text-[color:var(--ink-muted)]">
        Todo with Love — crafted with focus
      </div>
    </footer>
  );
}
